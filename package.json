{
  "name": "catopillsninja-gpt55",
  "version": "1.0.0",
  "description": "Neon Kitty Catcher: a camera-controlled Fruit Ninja inspired browser game.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "vite --host 127.0.0.1",
    "build": "vite build",
    "test": "playwright test",
    "test:report": "playwright show-report"
  },
  "dependencies": {
    "vite": "^8.0.16"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
